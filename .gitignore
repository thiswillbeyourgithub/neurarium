# Environment-specific deploy script (VPS creds via env vars); not committed.
/deploy.sh

# Per-instance metrics config (umami URL/id, DNT choice); not committed.
/docker/.env

# Personal, environment-specific Claude Code notes (deploy procedure, hostnames,
# etc.); loaded by Claude Code but not committed.
/CLAUDE.local.md

TODO.md

# Source books (copyrighted reference PDFs + their extracted per-page Markdown
# and indexes, used author-side for sourcing); large and not for distribution.
/sources/

__pycache__
.serena
